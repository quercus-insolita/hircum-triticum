shell: bash
commands:
  install:
    description: Install required go dependencies
    cmd: go get ./...
  compile:
    description: Build go binaries
    cmd: mkdir -p bin && go build -o bin/parser cmd/parser/main.go
